# Console Output

Prints suggestions and events to the terminal.

## Purpose

Simple output plugin for early testing and debugging.

## Usage

When the runtime emits a `suggestion.created` event, this plugin prints it to stdout.
